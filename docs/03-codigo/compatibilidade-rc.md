# Compatibilidade da Transição para 1.0.0

## Status

O RC foi encerrado. A versão estável `1.0.0` é o novo contrato público.

## Compatibilidade garantida

- namespace `sdd master`;
- `--json` sem banner;
- `update` funcional e estável;
- `release`, `deploy` e `implement` continuam guardas;
- plugins e skills seguem sem execução automática.

## Observação

Este arquivo permanece por histórico do RC. A política ativa pós-1.0 está em `suporte-e-politica-de-versao.md`.
